<%= form_for(marketing_team) do |f| %>
  <% if marketing_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marketing_team.errors.count, "error") %> prohibited this marketing_team from being saved:</h2>

      <ul>
      <% marketing_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :coolness %>
    <%= f.number_field :coolness %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
